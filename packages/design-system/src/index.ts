// Design tokens + primitive components (input, select, modal, toast, tree, table) grow
// out incrementally as the editor-ui (M8) needs them. N8nButton is the M1 placeholder.
export { default as N8nButton } from './components/N8nButton.vue';
